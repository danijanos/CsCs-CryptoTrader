version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: echo "Hello Circle Ci in CsCs-CryptoTrader"

workflows:
  version: 2
  commit-workflow:
    jobs:
      - build